"""QMS-specific document builders.

These are opinionated layouts for quality management documents.
Each builder produces a ForgeDoc Document configured for its
specific layout, then render() handles output format.

Install with: pip install forgedoc[qms]
"""
